from pait.field.base import BaseField


class Request(BaseField):
    """The type used to identify the field is Request, and users do not need to use it directly"""


class CbvClass(BaseField):
    """The type used to identify the field is Cbv class, and users do not need to use it directly"""


class PaitModel(BaseField):
    """The type used to identify the field is pait model, and users do not need to use it directly"""
